# TripTracer v0.0.1 Documentation Baseline Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 TripTracer `v0.0.1` 的镜像、端口、访问方式和初始化约定沉淀到项目文档中，为下一轮目录骨架与容器编排提供基线。

**Architecture:** 本次只修改文档，不创建运行文件。主文档 `CLAUDE.md` 作为项目白皮书承载版本约定，`docs/superpowers/specs/` 与 `docs/superpowers/plans/` 用于沉淀本轮设计与执行记录。

**Tech Stack:** Markdown, Monorepo documentation conventions

---

### Task 1: 补充 v0.0.1 文档基线

**Files:**
- Modify: `CLAUDE.md`
- Create: `docs/superpowers/specs/2026-06-26-v0.0.1-bootstrap-design.md`
- Create: `docs/superpowers/plans/2026-06-26-v0.0.1-documentation-baseline.md`

- [ ] **Step 1: 复核 v0.0.1 要求**

确认文档必须覆盖以下内容：
- 前端、后端、MySQL、Elasticsearch、Redis、MinIO 的镜像选型
- 中间件端口与访问方式
- 当前轮次不下载镜像、不启动容器

- [ ] **Step 2: 在 `CLAUDE.md` 中补充镜像与访问基线**

将以下结构写入 `### v0.0.1` 小节下：

```md
#### v0.0.1 文档交付边界
#### v0.0.1 镜像选型基线
#### v0.0.1 端口与访问方式约定
#### v0.0.1 中间件初始化约定
#### v0.0.1 目录与容器职责预留
#### v0.0.1 下一步落地项
```

- [ ] **Step 3: 写入设计文档**

在 `docs/superpowers/specs/2026-06-26-v0.0.1-bootstrap-design.md` 中记录：

```md
# TripTracer v0.0.1 Bootstrap Design
```

并覆盖目标、范围、非范围、镜像选型、访问基线、下一步计划。

- [ ] **Step 4: 自检文档一致性**

逐项检查：
- `CLAUDE.md` 与 spec 中的镜像版本一致
- 端口号一致
- 明确写出“本轮不创建 compose、不启动容器”

- [ ] **Step 5: 验证文档结果**

Run: `sed -n '1,260p' CLAUDE.md`
Expected: `v0.0.1` 小节包含镜像、端口、访问方式与初始化约定

Run: `sed -n '1,220p' docs/superpowers/specs/2026-06-26-v0.0.1-bootstrap-design.md`
Expected: 设计文档可独立说明本轮交付边界

- [ ] **Step 6: 提交说明给用户**

说明已完成文档更新，并指出下一轮可继续创建目录骨架与 `docker-compose.yml`。
